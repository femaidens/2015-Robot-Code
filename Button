/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package edu.wpi.first.wpilibj.templates;

import edu.wpi.first.wpilibj.Joystick;
import edu.wpi.first.wpilibj.Timer;

/**
 *
 * @author vincentk
 */
public class Button {
    private Joystick leftJoystick;
    private Joystick rightJoystick;
    private Joystick attackController;
    private Forklift forklift;
    private Timer clock;
    
public Button(Joystick left, Joystick right,Joystick atk, Forklift lift, Timer clock){
    leftJoystick = left;
    rightJoystick = right;
    attackController = atk;
    forklift = lift;
    clock = time;
}
public void configureButtons() {
   if (attackController.getRawButton(6)) {
        forklift.up();
   } 
   if (attackController.getRawButton(7)) {
        forklift.down();
   }
   if (attackController.getRawButton(4)) {
        forklift.binMode();
   }
   if (attackController.getRawButton(5)) {
        forklift.toteMode();
    }
}
